# source "https://rubygems.org"
# gem 'github-pages', group: :jekyll_plugins

# gem 'wdm', '>= 0.1.0' if Gem.win_platform?

# group :jekyll_plugins do
#     gem 'jekyll-feed'
#     gem 'jekyll-sitemap'
#     gem 'jekyll-paginate'
#     gem 'jekyll-seo-tag'
#     gem 'jekyll-archives'
#     gem 'kramdown'
#     gem 'rouge'
# end

source "https://rubygems.org"
gem 'github-pages', group: :jekyll_plugins

# Windows-specific gem for better performance
gem 'wdm', '>= 0.1.0' if Gem.win_platform?

# The github-pages gem already includes several plugins.
# If you add plugins here, ensure they are compatible with GitHub Pages.
group :jekyll_plugins do
  # Add any additional plugins you need here
  # Ensure they are compatible with GitHub Pages
end
